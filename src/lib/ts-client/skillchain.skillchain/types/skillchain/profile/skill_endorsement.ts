// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: skillchain/profile/skill_endorsement.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "skillchain.profile";

export interface SkillEndorsement {
  index: string;
  endorser: string;
  targetUser: string;
  skillName: string;
  endorsementType: string;
  comment: string;
  createdAt: number;
  skillTokensStaked: number;
  creator: string;
}

function createBaseSkillEndorsement(): SkillEndorsement {
  return {
    index: "",
    endorser: "",
    targetUser: "",
    skillName: "",
    endorsementType: "",
    comment: "",
    createdAt: 0,
    skillTokensStaked: 0,
    creator: "",
  };
}

export const SkillEndorsement: MessageFns<SkillEndorsement> = {
  encode(message: SkillEndorsement, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== "") {
      writer.uint32(10).string(message.index);
    }
    if (message.endorser !== "") {
      writer.uint32(18).string(message.endorser);
    }
    if (message.targetUser !== "") {
      writer.uint32(26).string(message.targetUser);
    }
    if (message.skillName !== "") {
      writer.uint32(34).string(message.skillName);
    }
    if (message.endorsementType !== "") {
      writer.uint32(42).string(message.endorsementType);
    }
    if (message.comment !== "") {
      writer.uint32(50).string(message.comment);
    }
    if (message.createdAt !== 0) {
      writer.uint32(56).uint64(message.createdAt);
    }
    if (message.skillTokensStaked !== 0) {
      writer.uint32(64).uint64(message.skillTokensStaked);
    }
    if (message.creator !== "") {
      writer.uint32(74).string(message.creator);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SkillEndorsement {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSkillEndorsement();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.index = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.endorser = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.targetUser = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.skillName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.endorsementType = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.comment = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.createdAt = longToNumber(reader.uint64());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.skillTokensStaked = longToNumber(reader.uint64());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.creator = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SkillEndorsement {
    return {
      index: isSet(object.index) ? globalThis.String(object.index) : "",
      endorser: isSet(object.endorser) ? globalThis.String(object.endorser) : "",
      targetUser: isSet(object.targetUser) ? globalThis.String(object.targetUser) : "",
      skillName: isSet(object.skillName) ? globalThis.String(object.skillName) : "",
      endorsementType: isSet(object.endorsementType) ? globalThis.String(object.endorsementType) : "",
      comment: isSet(object.comment) ? globalThis.String(object.comment) : "",
      createdAt: isSet(object.createdAt) ? globalThis.Number(object.createdAt) : 0,
      skillTokensStaked: isSet(object.skillTokensStaked) ? globalThis.Number(object.skillTokensStaked) : 0,
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
    };
  },

  toJSON(message: SkillEndorsement): unknown {
    const obj: any = {};
    if (message.index !== "") {
      obj.index = message.index;
    }
    if (message.endorser !== "") {
      obj.endorser = message.endorser;
    }
    if (message.targetUser !== "") {
      obj.targetUser = message.targetUser;
    }
    if (message.skillName !== "") {
      obj.skillName = message.skillName;
    }
    if (message.endorsementType !== "") {
      obj.endorsementType = message.endorsementType;
    }
    if (message.comment !== "") {
      obj.comment = message.comment;
    }
    if (message.createdAt !== 0) {
      obj.createdAt = Math.round(message.createdAt);
    }
    if (message.skillTokensStaked !== 0) {
      obj.skillTokensStaked = Math.round(message.skillTokensStaked);
    }
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SkillEndorsement>, I>>(base?: I): SkillEndorsement {
    return SkillEndorsement.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SkillEndorsement>, I>>(object: I): SkillEndorsement {
    const message = createBaseSkillEndorsement();
    message.index = object.index ?? "";
    message.endorser = object.endorser ?? "";
    message.targetUser = object.targetUser ?? "";
    message.skillName = object.skillName ?? "";
    message.endorsementType = object.endorsementType ?? "";
    message.comment = object.comment ?? "";
    message.createdAt = object.createdAt ?? 0;
    message.skillTokensStaked = object.skillTokensStaked ?? 0;
    message.creator = object.creator ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
